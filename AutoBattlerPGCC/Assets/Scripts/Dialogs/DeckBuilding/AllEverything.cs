using System.Collections.Generic;
using UnityEngine;

public class AllEverything : MonoBehaviour
{
    
    public List<CharacterDefinition> allCharacterDefs;
    public List<WeaponDefinition> allWeapons;
    public List<AbilityDefinition> allUniqueAbilities;
    public List<AbilityDefinition> allNormalAbilities;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
